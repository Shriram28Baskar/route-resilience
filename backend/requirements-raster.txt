# Optional: raster/DEM handling. Needed only for /simulate/flood* (DEM sampling)
# and GeoTIFF tile upload. The resilience engine runs without it.
-r requirements-core.txt
rasterio==1.3.10
scikit-image==0.23.2
