# Quantitative claim audit

Every number this repository states or displays, classified. Generated by
inspection of the working tree; re-run the audit whenever a figure is added.

**Classification**

| Class | Meaning |
|---|---|
| **MEASURED** | computed from a real input artifact present on disk, reproducible by committed code |
| **DERIVED** | computed from a measured input **plus** constants that are not themselves measured |
| **SYNTHETIC** | produced by a placeholder model with no real input behind it |
| **ILLUSTRATIVE** | a tuning parameter or display threshold; not a claim about the world |
| **UNSUPPORTED** | asserted with no computation behind it anywhere in the repo — **deleted** |

The rule applied throughout: an unsupported number is **removed**, never replaced
with a more plausible one.

---

## 1. README — all UNSUPPORTED, all deleted

| Claim | Class | Disposition |
|---|---|---|
| IoU 0.73 on SpaceNet Roads | UNSUPPORTED | deleted — no checkpoint, no dataset, no eval script in repo |
| F1-Score 0.81 | UNSUPPORTED | deleted — same |
| "+12% IoU retention vs baseline at 40% occlusion" | UNSUPPORTED | deleted — no occlusion benchmark exists |
| Precision@5 = 4/5 (Chennai 2015 / Kerala 2018) | UNSUPPORTED | deleted — `04_validation.ipynb` has zero code, no ground truth |
| Precision@10 = 8/10 | UNSUPPORTED | deleted — same |
| "13,486 intersections" | UNSUPPORTED | deleted — from a cache now rejected on load; OSM extract not committed |
| "16,000+ road segments" | UNSUPPORTED | deleted — same |
| "24 emergency facilities" | UNSUPPORTED | deleted — required a live Overpass query |
| "44 nodes failed / 5 partitions" | UNSUPPORTED | deleted — not reproducible |
| "44,552 residents impacted" | UNSUPPORTED | deleted — `44 × 1008`, a constant not a census join |
| "312 vulnerable intersections" | UNSUPPORTED | deleted — required an absent DEM |
| "answers that in 3 seconds" | UNSUPPORTED | deleted — no latency benchmark exists |

Each appears in README only inside the withdrawal table, with its reason.

## 2. Frontend

| Location | Claim | Class | Disposition |
|---|---|---|---|
| `simulate/page.tsx` | "(Confidence: 78%)" on cascade | UNSUPPORTED | deleted (P0) — no uncertainty quantification exists anywhere |
| `simulate/page.tsx` | "Estimated remaining cascade: 0.4×–0.8× additional failures" | UNSUPPORTED | deleted (P0) — frontend arithmetic, not a model output |
| `simulate/page.tsx` | "Maximum iterations configured: 15" | UNSUPPORTED | deleted (P0) — actual default is 3 |
| `simulate/page.tsx` | "Routes Blocked = nodes × 3" | UNSUPPORTED | deleted (P0) |
| `simulate/page.tsx` | infrastructure labels ("Hospital Access", …) | UNSUPPORTED | deleted (P0) — round-robin by list index, no facility layer |
| `simulate/page.tsx` | `totalNodes ?? 13486` | UNSUPPORTED | deleted (P1) — hardcoded Bengaluru count applied to any graph |
| `simulate/page.tsx` | `percolation_threshold × 13486` + "drops precipitously below 0.70" | UNSUPPORTED | deleted (P1) — replaced with the measured percentage |
| all numeric panels | every displayed figure | — | now carries a `ProvenanceBadge` showing MEASURED/DERIVED/SYNTHETIC |
| 503 responses | — | — | now render `DataUnavailablePanel` naming the missing artifact, not an empty chart |

## 3. Backend constants — the DERIVED chain

These are real code paths. None is measured; each is now declared in the
`data_provenance.assumptions` list of every response that depends on it.

| Constant | Value | Class | Where declared |
|---|---|---|---|
| `TOTAL_POPULATION` | 13,600,000 | DERIVED | `population.py` — AOI total, uniformly distributed across LCC nodes |
| population per node | 1,008 | DERIVED | `/simulate/flood`, `/simulate/relief-camps` |
| `cost_estimate_usd` | 15,000 / flooded node | DERIVED | `/simulate/flood` — flat unsourced coefficient |
| `_AVG_HOURLY_WAGE_INR` | 285 | DERIVED | `/simulate/traffic-impact` (503 without OD matrix) |
| `_DETOUR_PENALTY_PER_KM` | 180 INR | DERIVED | same |
| `_BENGALURU_DAILY_COMMUTERS` | 9,200,000 | DERIVED | same |
| `_WORKDAYS_PER_YEAR` | 250 | DERIVED | same |
| gridlock coefficient | `fraction × 300` min | DERIVED | same — invented coefficient, now declared |
| trip-volume proxy | `max(betweenness) × 0.25` | DERIVED | same — betweenness is a path count, not a flow |
| rehab cost | ₹8M/₹25M per km | DERIVED | `temporal_degradation.py` (503 without road conditions) |
| `DEFAULT_PENALTY_S` | 3,600 s | ILLUSTRATIVE | `resilience.py` — arbitrary by construction; reported per response with `ri_floor`; ranking invariance tested |
| `ALT_EDGE_PENALTY` | 4.0 | ILLUSTRATIVE | route-alternative heuristic |
| `_DAMPENING_FACTOR` | 0.0 | ILLUSTRATIVE | off by default; returned per cascade step |
| `MAX_BRIDGE_DISTANCE` | 0.005° | ILLUSTRATIVE | MST healing tuning |
| `ANGLE_PENALTY_WEIGHT` | 0.3 | ILLUSTRATIVE | MST healing tuning |
| `DEFAULT_K` | 50 pivots (5 above 5000 nodes) | ILLUSTRATIVE | declared per response; widens approximation error |
| `MIN_BRANCH_PX` / `CLOSE_RADIUS` | 10 / 3 px | ILLUSTRATIVE | skeletonisation tuning |
| resilience score weights | `0.7 × LCC + 0.3` | ILLUSTRATIVE | display heuristic, uncalibrated — declared |
| equity score penalties | −5/desert, −10/HIGH ward | ILLUSTRATIVE | display heuristic — declared |
| desert threshold | 5,000 m straight-line | ILLUSTRATIVE | declared; note it is **not** network distance |

### Deleted in this pass

| Claim | Class | Reason |
|---|---|---|
| `"cost_estimate": "₹2.5 Cr - ₹5.0 Cr"` | UNSUPPORTED | no costing model exists; set to `null` |
| `"cost_estimate": "$1.2M - $3.0M"` | UNSUPPORTED | same |
| `hospitals_affected = int(n × 0.005)` | UNSUPPORTED | deleted (P1) — no facility layer joined |
| `emergency_stations_affected = int(n × 0.002)` | UNSUPPORTED | deleted (P1) — same |

## 4. Notebooks

| Notebook | Class | Status |
|---|---|---|
| `01_data_exploration.ipynb` | — | plotting only; requires SpaceNet data not in repo |
| `02_model_evaluation.ipynb` | — | contains real IoU/Dice code, but needs an absent checkpoint. Cannot be executed; produces no committed claim |
| `03_graph_analysis.ipynb` | — | requires the absent OSM extract |
| `04_validation.ipynb` | UNSUPPORTED | **one markdown cell, zero code.** The source of the withdrawn Precision@K figures. Header now states no validation has been run |

## 5. What CAN be regenerated today

All on synthetic topologies. No claim about a real road network is currently
reproducible here — the OSM extract is not committed and this environment cannot
reach Overpass.

| Claim | Class | Regenerated by |
|---|---|---|
| RI monotonic under nested ablation | MEASURED | `tests/test_resilience_index.py` |
| RI floor = `baseline_mean / penalty_s`; RI not cross-network comparable | MEASURED | same |
| Intervention ranking invariant across penalties 1800/3600/7200 s | MEASURED | same |
| Infinite-penalty limit is degenerate (all RI → 0) | MEASURED | same — a **negative result**, retained |
| Decomposition independent of `penalty_s` | MEASURED | same |
| Low RI can coexist with path inflation ≈ 1.0 (severance, not slowdown) | MEASURED | same |
| Targeted attack does **not** dominate random failure on a homogeneous lattice | MEASURED | `tests/test_no_clamps.py` — **negative result**, retained |
| Targeted attack **does** dominate on a chokepoint topology | MEASURED | same |
| Cascade counts are non-monotonic without forced decay | MEASURED | same |
| Route endpoint injects no unrequested failures (0/40) and does not crash (0/40) | MEASURED | same |
| Mismatched / unfingerprinted centrality cache is refused | MEASURED | `tests/test_reproducibility.py` |
| ML pipeline cannot mutate the analysis graph | MEASURED | same |

## 6. Graph artifacts

| Artifact | Class | Status |
|---|---|---|
| `data/graphs/osm_fallback.gpickle` | — | **not committed.** Required for any real-network result |
| `data/graphs/osm_fallback_criticality.pickle` | UNSUPPORTED as a result | 13,486 nodes / 19,117 edge entries, no fingerprint, provenance unknown. **Rejected on load.** Retained only as a record of where the withdrawn README figures came from. See `backend/data/README.md` |
| "Bengaluru topology proxy" used in P0/P1 measurement reports | SYNTHETIC | a topology reconstructed from the above pickle's edge-betweenness **keys**. Uniform weights, no geometry, no speed limits. **Not a real weighted road graph** and never described as one. Used only for route-sweep counts, which depend on topology alone |
